{% include 'incl/header.twig' %}

<body>
    {% include 'incl/nav.twig' %}
    <div class="container">
        <div class="side-bar">
             {% for page in pages("analysis")|sort_by("time")|reverse if not page.hidden %}
                    <h3><a href="{{ page.url }}">{{ page.title }}</a></h3>
            {% endfor %}
        </div>
        <div class="blog">
        {{content}}
        </div>
    </div>
    {% include 'incl/footer.twig' %}
</body>

</html>
